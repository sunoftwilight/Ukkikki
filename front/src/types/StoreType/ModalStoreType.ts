export interface InviteStoreType {
  inviteOpen : boolean;
  setInviteOpen: () => void;
}

export interface MemberStoreType {
  memberOpen : boolean;
  setMemberOpen: () => void;
}

export interface FolderStoreType {
  folderOpen: boolean;
  setFolderOpen: () => void;
}