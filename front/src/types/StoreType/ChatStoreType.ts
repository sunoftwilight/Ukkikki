export interface LoadingStoreType {
  isLoading: boolean;
  setIsLoading: (Is: boolean) => void;
}

export interface LastStoreType {
  isLast: boolean;
  setIsLast: (Is: boolean) => void;
}

// export interface FocusStoreType {
//   isFocus: boolean;
//   setIsFocus: () => void;
// }